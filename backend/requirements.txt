Flask==3.1.3

# Everything else (sqlite3, csv, dataclasses, unittest) is Python stdlib - no
# ORM, no request-validation framework, no test runner dependency. Deliberate:
# see docs/ARCHITECTURE.md for why, and for the Postgres/SQLAlchemy swap path
# if you're extending this beyond the demo.

# Optional - only needed if INSIGHT_PROVIDER=vertex-ai in production.
# Not required for local dev/demo, which runs on MockProvider.
# google-cloud-aiplatform==1.71.1
